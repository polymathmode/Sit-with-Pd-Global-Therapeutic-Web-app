$kind: http-request
description: Admin only. Paginated list of all registered users with
  purchase/booking counts.
url: "{{baseUrl}}/api/admin/users?page=1&limit=20"
method: GET
headers:
  Authorization: Bearer {{authToken}}
queryParams:
  page: "1"
  limit: "20"
order: 2000
