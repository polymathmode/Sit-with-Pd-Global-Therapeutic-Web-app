$kind: http-request
description: Admin only. Partial update of a tier (send only fields to change).
url: "{{baseUrl}}/api/camps/{{campId}}/tiers/{{campTierId}}"
method: PATCH
headers:
  Authorization: Bearer {{authToken}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "label": "Couple",
      "price": 750,
      "seatsPerUnit": 2,
      "isFeatured": true,
      "order": 1
    }
order: 7200
