$kind: http-request
description: Admin only. Update caption and/or display order for a gallery image.
url: "{{baseUrl}}/api/camps/{{campId}}/images/{{campImageId}}"
method: PATCH
headers:
  Authorization: Bearer {{authToken}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "caption": "Walking in nature",
      "order": 2
    }
order: 7500
