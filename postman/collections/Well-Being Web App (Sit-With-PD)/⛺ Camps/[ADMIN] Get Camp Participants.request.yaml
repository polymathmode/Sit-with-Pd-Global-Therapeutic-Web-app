$kind: http-request
description: Admin only. Returns applicants for a camp with tier, payment
  status, participantCount, and applicantDetails.
url: "{{baseUrl}}/api/camps/{{campId}}/participants"
method: GET
headers:
  Authorization: Bearer {{authToken}}
order: 7000
