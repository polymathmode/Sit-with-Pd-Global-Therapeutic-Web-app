$kind: http-request
description: Admin only. Removes a gallery image record (does not delete from Cloudinary).
url: "{{baseUrl}}/api/camps/{{campId}}/images/{{campImageId}}"
method: DELETE
headers:
  Authorization: Bearer {{authToken}}
order: 7600
