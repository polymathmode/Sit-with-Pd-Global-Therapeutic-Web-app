$kind: http-request
description: |-
  Published programs only (`isPublished` true).
  Optional `?search=` — substring on title/description (case-insensitive, max 100 chars).
  Optional `?category=` — LEADERS | STUDENTS | PROFESSIONALS (matches ProgramCategory / "program type").
  No auth required.
url: "{{baseUrl}}/api/programs"
method: GET
auth:
  type: noauth
order: 1000
