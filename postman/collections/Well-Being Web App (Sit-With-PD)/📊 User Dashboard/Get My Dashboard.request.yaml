$kind: http-request
description: Returns the logged-in user's purchases, camp registrations, and
  consultation bookings in one call.
url: "{{baseUrl}}/api/dashboard"
method: GET
headers:
  Authorization: Bearer {{authToken}}
order: 1000
