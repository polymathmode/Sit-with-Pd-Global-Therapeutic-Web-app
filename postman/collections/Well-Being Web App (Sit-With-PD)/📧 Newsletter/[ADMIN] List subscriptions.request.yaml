$kind: http-request
description: |-
  Admin. Paginated list of newsletter subscriptions (no unsubscribeToken in payload).
  Optional `search` — email or source substring (max 100 chars).
  Optional `status` — all | active (not unsubscribed) | unsubscribed.
url: "{{baseUrl}}/api/admin/newsletter/subscriptions?page=1&limit=20&status=all"
method: GET
headers:
  Authorization: Bearer {{authToken}}
order: 2650
