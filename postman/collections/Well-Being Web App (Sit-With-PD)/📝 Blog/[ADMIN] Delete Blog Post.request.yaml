$kind: http-request
description: >-
  Admin only. Permanently deletes a post. Set `blogPostId` to the id returned
  by create or from **List All Blog Posts**.
url: "{{baseUrl}}/api/admin/blog/{{blogPostId}}"
method: DELETE
auth:
  type: noauth
headers:
  Authorization: Bearer {{authToken}}
order: 7000
